/**
 * Toggle DEV_FAST to true to run through the full exercise flow quickly.
 * Each exercise will use only 2 items and timers will be shortened.
 * Set to false (or remove) before shipping.
 */
export const DEV_FAST = false;
